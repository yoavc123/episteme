---
phase: 01-audio-ebook-sync
plan: 08
type: execute
wave: 4
depends_on: [01-audio-ebook-sync-02, 01-audio-ebook-sync-06]
files_modified:
  - app/src/main/java/com/aryan/reader/audio/EpubMediaOverlayParser.kt
  - app/src/main/java/com/aryan/reader/audio/SyncedAudioPlaybackManager.kt
  - app/src/main/java/com/aryan/reader/epubreader/EpubReaderScreen.kt
  - app/src/main/java/com/aryan/reader/epubreader/EpubReaderControls.kt
  - app/src/test/java/com/aryan/reader/audio/EpubMediaOverlayParserTest.kt
autonomous: true
requirements: [AES-04, AES-05]
must_haves:
  truths:
    - "The reader can detect EPUB3 Media Overlays in a synced EPUB"
    - "The reader can play embedded/copyable audio clips referenced by SMIL"
    - "Playback can move the current reader location/highlight according to SMIL text refs"
  artifacts:
    - path: "app/src/main/java/com/aryan/reader/audio/EpubMediaOverlayParser.kt"
      provides: "OPF/SMIL parser for EPUB3 media overlays"
    - path: "app/src/main/java/com/aryan/reader/audio/SyncedAudioPlaybackManager.kt"
      provides: "Media3-backed playback and clip navigation for synced EPUB audio"
    - path: "app/src/main/java/com/aryan/reader/epubreader/EpubReaderScreen.kt"
      provides: "Reader integration for media overlay availability and navigation"
  key_links:
    - from: "EpubMediaOverlayParser.kt"
      to: "SyncedAudioPlaybackManager.kt"
      via: "parsed SMIL clip list drives Media3 playback"
      pattern: "MediaOverlayClip"
    - from: "SyncedAudioPlaybackManager.kt"
      to: "EpubReaderScreen.kt"
      via: "clip text refs navigate/highlight reader content"
      pattern: "on.*Locator|navigate"
---

<objective>
Teach the Android reader to parse and play EPUB3 Media Overlays produced by sync.

Purpose: Saving a synced EPUB3 is not enough; Episteme's reader needs to expose the readaloud experience from the generated file.
Output: Media Overlay parser, playback manager, EPUB reader integration, and parser tests.
</objective>

<execution_context>
@C:/Users/yoavc/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/yoavc/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-audio-ebook-sync/01-audio-ebook-sync-02-SUMMARY.md
@.planning/phases/01-audio-ebook-sync/01-audio-ebook-sync-06-SUMMARY.md
@app/src/main/java/com/aryan/reader/epubreader/EpubReaderScreen.kt
@app/src/main/java/com/aryan/reader/epubreader/EpubReaderControls.kt
@app/build.gradle.kts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Parse OPF and SMIL media overlays</name>
  <files>app/src/main/java/com/aryan/reader/audio/EpubMediaOverlayParser.kt, app/src/test/java/com/aryan/reader/audio/EpubMediaOverlayParserTest.kt</files>
  <action>Implement a parser that reads an EPUB zip, finds the OPF through `META-INF/container.xml`, finds manifest items with `media-overlay`, parses linked SMIL files, resolves relative text/audio hrefs safely, and returns ordered clips with content href, element id, audio href, clipBegin, clipEnd, and duration. Tests must cover relative paths, missing overlays, malformed SMIL, and clips generated by `SharedEpubMediaOverlayWriter` fixtures.</action>
  <verify>`./gradlew.bat :app:testOssDebugUnitTest --tests "com.aryan.reader.audio.EpubMediaOverlayParserTest"`</verify>
  <done>Parser can detect/read media overlays from generated synced EPUBs without extracting unsafe paths.</done>
</task>

<task type="auto">
  <name>Task 2: Add Media3 playback manager for synced audio clips</name>
  <files>app/src/main/java/com/aryan/reader/audio/SyncedAudioPlaybackManager.kt</files>
  <action>Create a Media3-backed manager that prepares audio assets from EPUB entries into app cache when needed, plays clip ranges in SMIL order, supports play/pause/stop/seek next/previous, emits current clip state, and releases ExoPlayer cleanly. It must not conflict with existing TTS playback sessions: stop/pause synthetic TTS before synced audio starts and release synced audio when reader closes.</action>
  <verify>`./gradlew.bat :app:assembleOssDebug` compiles and unit tests/fakes cover clip ordering and state transitions where feasible.</verify>
  <done>Synced audio can be controlled independently with the same lifecycle expectations as reader TTS.</done>
</task>

<task type="auto">
  <name>Task 3: Connect synced playback to EPUB reader navigation</name>
  <files>app/src/main/java/com/aryan/reader/epubreader/EpubReaderScreen.kt, app/src/main/java/com/aryan/reader/epubreader/EpubReaderControls.kt</files>
  <action>When the opened EPUB has media overlays, load overlay metadata and expose availability/state to reader controls. During playback, navigate/highlight the current sentence id using existing chapter WebView/locator mechanisms; if exact element id navigation is unavailable, scroll to chapter and use the closest existing text highlight mechanism without breaking normal reading. Keep existing TTS overlay controls intact for synthetic TTS.</action>
  <verify>`./gradlew.bat :app:testOssDebugUnitTest --tests "com.aryan.reader.epubreader.*"` for affected unit tests and `./gradlew.bat :app:assembleOssDebug`.</verify>
  <done>Opening a synced EPUB allows media overlay playback and visible progression through reader content.</done>
</task>

</tasks>

<verification>
Run `./gradlew.bat :app:testOssDebugUnitTest --tests "com.aryan.reader.audio.EpubMediaOverlayParserTest"` and `./gradlew.bat :app:assembleOssDebug`.
</verification>

<success_criteria>
Synced EPUB3 files created by the app can be recognized and played in the Android EPUB reader with SMIL-driven clip progression.
</success_criteria>

<output>
After completion, create `.planning/phases/01-audio-ebook-sync/01-audio-ebook-sync-08-SUMMARY.md`
</output>
